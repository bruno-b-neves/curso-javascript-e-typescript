let num1 = 9.55467;

// Gera o dado númerio Infinity | Em outras linguagens gera um erro 
// console.log(100 / 0);

// Retorna o maior número inteiro menor ou igual ao argumento numérico.
// let num2 = Math.floor(num1)

// Retorna o maior número inteiro maior ou igual ao argumento numérico.
// let num2 = Math.ceil(num1);

// Retorna uma expressão numérica fornecida arredondada para o número inteiro mais próximo.
// let num2 = Math.round(num1);

// console.log(num2);

// Retorna o maior valor de um conjunto de expressões numéricas fornecidas.
// console.log(Math.max(1,2,3,4,5,-10,-50,1500,9,8,7,6));

// Retorna o menor valor de um conjunto de expressões numéricas fornecidas.
// console.log(Math.min(1,2,3,4,5,-10,-50,1500,9,8,7,6));

// Retorna um número pseudoaleatório entre 0 e 1.
// console.log(Math.random())

// const aleatorio = Math.round(Math.random() * (10 - 5) + 5);

// PI. É a razão entre a circunferência de um círculo e o seu diâmetro.
// console.log(Math.PI);

// Retorna o valor de uma expressão base elevada a uma potência especificada.
// console.log(Math.pow(2, 10))

num1 = 9;
console.log(num1 ** (1/2)); // Retorna a Raiz Quadrada de 9 - Forma Simples
console.log(num1 ** 0.5); // Retorna a Raiz Quadrada de 9 - Forma Simples
console.log(Math.pow(num1, 0.5)) // Retorna a Raiz Quadrada de 9